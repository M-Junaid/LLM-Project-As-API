Clone the repository.
Install dependencies using pip install -r requirements.txt.
Run app.py to start the API server.
Access the Swagger UI documentation at http://localhost:8000/docs to explore available endpoints.
Use client.py to interact with the APIs and receive text generation outputs.
